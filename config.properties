# Cấu hình hệ thống quản lý sách thư viện

# Cấu hình Database
database.host=localhost
database.port=27017
database.name=library_management

# Cấu hình Server
server.port=8888
server.max_clients=100

# Cấu hình mặc định
default.admin.email=dainam@gmail.com
default.admin.password=dainam
default.admin.name=Đại Nam

# Cấu hình mượn sách
borrow.default_days=14
borrow.max_days=365
borrow.warning_days=3
